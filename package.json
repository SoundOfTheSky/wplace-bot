{
  "name": "wplacebot",
  "version": "1.0.0",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "scripts": {
    "start": "bun build ./src/index.ts --outfile=dist.user.js && (cat script.txt dist.user.js) > tmp && mv tmp dist.user.js",
    "lint": "eslint \"src/**/*{.js,.ts}\" --fix && tsc"
  },
  "devDependencies": {
    "@softsky/configs": "^1.3.4"
  },
  "dependencies": {
    "@softsky/utils": "^2.7.3"
  }
}
