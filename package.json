{
  "name": "wplacebot",
  "version": "1.0.0",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "scripts": {
    "start": "bun src/build.ts",
    "copy": "bun start && wl-copy < dist.user.js",
    "test": "bun test",
    "lint": "stylelint \"src/**/*.{css,scss,sass}\" --fix && eslint \"src/**/*{.js,.ts}\" --fix && tsc"
  },
  "devDependencies": {
    "@softsky/configs": "^1.5.2",
    "@types/bun": "^1.3.14"
  },
  "dependencies": {
    "@softsky/utils": "^2.8.4"
  }
}
